# Restrict this address with your host firewall; the app has no authentication.
HOST = '0.0.0.0'
PORT = 8080

# Optional directory containing one generated plain-text file per tab.
MIRROR_DIR = './mirror'
